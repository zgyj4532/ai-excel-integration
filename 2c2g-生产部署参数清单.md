# 2c2g Linux 生产部署参数清单

这份清单面向 2 核 2G 内存的 Linux 服务器，目标是把当前项目稳定跑起来，并尽量降低内存、CPU 和磁盘压力。

## 1. 部署策略

- 只部署一个 Java 进程，不要在同机再跑 Redis、MySQL、Nginx 以外的额外重服务。
- 前端建议在 CI 或构建机上提前打包，服务器只接收后端 jar 和静态资源成品，不要在生产机上重复 `npm install` 和 `npm run build`。
- 机器只承载低并发访问，适合少量内部用户，不适合高频批量分析。

## 2. JVM 参数

建议从保守配置开始：

```bash
JAVA_OPTS="-Xms256m -Xmx768m -XX:MaxMetaspaceSize=192m -XX:+UseG1GC -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/opt/ai-excel-integration/heapdump.hprof"
```

说明：

- `-Xmx768m` 是 2G 机器的较稳起点，避免把系统内存吃满。
- `MaxMetaspaceSize` 不要设太大，Spring Boot 启动后元空间也会占用不少。
- 开启 OOM dump，便于排查 Excel 解析或 AI 调用时的内存峰值问题。

如果实例还要同时跑轻量 Nginx 或其他常驻服务，可把 `-Xmx` 再降到 `512m` 到 `640m` 区间。

## 3. 启动命令

推荐用 `java -jar` 配合环境变量，而不是直接用 `mvn spring-boot:run`：

```bash
export SERVER_PORT=8080
export SPRING_PROFILES_ACTIVE=prod
export QWEN_API_KEY=your_real_key
export QWEN_MODEL_NAME=qwen-max
export QWEN_API_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

java $JAVA_OPTS -jar /opt/ai-excel-integration/target/ai-excel-integration-0.0.1-SNAPSHOT.jar
```

## 4. Nginx 反向代理

如果要暴露到公网，建议加一层 Nginx：

- 由 Nginx 监听 80/443。
- Spring Boot 只监听本地 `127.0.0.1:8080`。
- 上传、下载和 SSE/WebSocket 都走反代。

建议保留的关键配置：

```nginx
proxy_read_timeout 3600s;
proxy_send_timeout 3600s;
client_max_body_size 20m;
proxy_buffering off;
```

说明：

- `proxy_buffering off` 对流式输出更友好。
- `client_max_body_size` 要和后端上传限制一致，避免前后端不一致。

## 5. 环境变量

建议至少准备这些变量：

```bash
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=prod
QWEN_API_KEY=...
QWEN_MODEL_NAME=qwen-max
QWEN_API_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
MAX_FILE_SIZE=10MB
MAX_REQUEST_SIZE=10MB
```

建议值：

- `SERVER_PORT` 固定一个端口，方便反代和防火墙配置。
- `MAX_FILE_SIZE` / `MAX_REQUEST_SIZE` 不建议在 2G 机器上放太大，默认 10MB 比较稳。
- 如果业务允许，上传限制可以再收紧到 5MB 到 8MB。

## 6. Redis 策略

- 如果 Redis 只是辅助会话缓存，优先使用外部 Redis，别和 Java 服务挤在同一台 2G 机器上。
- 如果必须同机部署，建议限制 Redis 占用，避免和 JVM 抢内存。
- 没有 Redis 也要确认系统在降级模式下能正常启动，不要因为 Redis 不可用直接阻塞主服务。

## 7. 文件与磁盘

- 将上传目录固定到独立路径，例如 `/opt/ai-excel-integration/storage/uploads`。
- 对上传文件和导出文件做定期清理，避免磁盘被长期占满。
- 日志目录建议单独设置并配合 logrotate。

建议检查：

- `/opt/ai-excel-integration/storage` 是否有足够空间。
- `/tmp` 是否会被大量临时 Excel 文件占满。

## 8. 后端运行参数

建议开启这些基础 JVM/系统保护：

- `-XX:+HeapDumpOnOutOfMemoryError`
- `-XX:HeapDumpPath=...`
- `-Dfile.encoding=UTF-8`
- `-Duser.timezone=Asia/Shanghai`

如果需要更稳的 GC 行为，可以保持默认 G1，不建议在 2G 机器上过度调优复杂 GC 参数。

## 9. 前端构建策略

- 前端已经适合打包成静态资源随后端发布。
- 生产机上不要再做完整前端构建，避免 Node、npm 和 Vite 额外占内存。
- 当前构建链路已经按项目配置做了 chunk 拆分，部署时直接使用打包后的静态资源即可。

## 10. systemd 服务建议

可以用 systemd 托管后端服务：

```ini
[Unit]
Description=AI Excel Integration
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/ai-excel-integration
EnvironmentFile=/opt/ai-excel-integration/.env
Environment=JAVA_OPTS=-Xms256m -Xmx768m -XX:MaxMetaspaceSize=192m -XX:+UseG1GC
ExecStart=/usr/bin/java $JAVA_OPTS -jar /opt/ai-excel-integration/target/ai-excel-integration-0.0.1-SNAPSHOT.jar
Restart=always
RestartSec=5
LimitNOFILE=65535

[Install]
WantedBy=multi-user.target
```

## 11. 防火墙与端口

- 只开放 Nginx 的 80/443。
- Spring Boot 内网端口不直接暴露公网。
- 如果必须直连 Spring Boot，确认安全组和防火墙只放行指定源 IP。

## 12. 监控与告警

至少监控这些指标：

- JVM 堆内存使用率
- CPU 使用率
- 磁盘剩余空间
- 上传目录文件数量
- Qwen API 请求失败率和超时率

建议最低告警阈值：

- 内存持续高于 85%
- 磁盘低于 20%
- API 连续失败超过 3 次

## 13. 已知风险点

- 上传大 Excel 或高频分析时，2G 内存仍可能触发卡顿。
- AI 请求超时会直接影响用户体验，尤其是外网不稳定时。
- 如果 Redis 和 Java 同机，内存压力会明显增加。
- 如果同时多用户触发导出、分析和上传，CPU 峰值会比较高。

## 14. 发布前检查

- `[ ]` 后端能通过 `mvn package` 正常构建。
- `[ ]` 前端静态资源已预构建并随 jar 一起发布。
- `[ ]` `.env` 中的 `QWEN_API_KEY`、`SERVER_PORT`、文件大小限制已填写。
- `[ ]` `storage/uploads` 目录已创建并有写权限。
- `[ ]` systemd 服务已配置自动重启。
- `[ ]` Nginx 反代和上传大小限制已同步。
- `[ ]` 服务器未同时部署额外高内存服务。

## 15. 建议的起始配置

如果你要直接上线，建议先用这一组最稳参数：

```bash
JAVA_OPTS="-Xms256m -Xmx768m -XX:MaxMetaspaceSize=192m -XX:+UseG1GC"
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=prod
MAX_FILE_SIZE=10MB
MAX_REQUEST_SIZE=10MB
```

这是 2c2g 机器上相对保守、可落地的起点。后续如果监控显示余量充足，再考虑把 `-Xmx` 提到 `896m`。
